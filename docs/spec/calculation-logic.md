# 計算ロジック仕様書

> 対象: ROI診断アプリ「またたび計算機」 / 関連Issue: #1 / マスター設計書: [`.claude/ROI診断アプリ「またたび計算機」開発・設計ドキュメント一式.md`](../../.claude/ROI診断アプリ「またたび計算機」開発・設計ドキュメント一式.md) §3.1

本ドキュメントは、マスター設計書 §3.1「計算ロジック詳細」を実装可能なレベルまで確定するための仕様書です。Issue #2〜#5（入力フォーム／グラフ／警告コピー／PDF）は本仕様を前提に設計されます。

---

## 1. 前提と目的

### 1.1 目的
- ROI 計算式で使用される固定値の扱い（固定 / デフォルト値 / 可変）を確定する。
- 「内製化状況」入力の選択肢と、計算式への反映方法を確定する。
- 最終計算式を擬似コードレベルで明文化し、後続実装（Issue #6以降）が参照できる状態にする。

### 1.2 設計原則
- **商談テンポ優先**: 通常フォームは 5 項目に留め、タブレット商談で快適に入力できること。
- **説明責任**: 経営者に数値の根拠を問われた時、営業担当が即答できる前提値設計であること。
- **誠実な結果**: 既に理想形に近い顧客（例: 完全内製）には、誠実に「削減余地なし」と表示できること。

---

## 2. 固定値一覧と扱い

| 変数 | 値 | 扱い | 理由（出典） |
|---|---|---|---|
| 試算期間 (`MONTHS_IN_PERIOD`) | 36 ヶ月 | **固定** | 商品コンセプト「3年間の止血」に結合。可変化は不可。 |
| 試算年数 (`YEARS_IN_PERIOD`) | 3 年 | **固定** | 同上。中期経営計画の標準期間と一致。 |
| 年あたり改修回数 (`REPAIRS_PER_YEAR`) | 3 回 | **固定** | システム保守業界通念（四半期改修＋軽微 1 回除外）。詳細出典は §3 参照。 |
| 年あたり月数 (`MONTHS_PER_YEAR`) | 12 ヶ月 | **固定** | 暦定数。 |
| 時給単価 (`hourlyWage`) | 2,500 円/時 | **デフォルト値（可変）** | 賃金構造基本統計調査 令和7年速報の所定内給与額に賞与・社会保険料事業主負担を加味した中小企業ホワイトカラー実質時給レンジ（2,500〜2,800 円）の下限値。詳細出典は §3 参照。 |
| 1日あたり作業時間 (`hoursPerDay`) | 2 時間/日 | **デフォルト値（可変）** | BPO テクノロジー「会社員の業務実態調査」のノンコア業務時間中央値レンジ（1〜3 時間/日）の中央値。詳細出典は §3 参照。 |
| 月あたり稼働日数 (`daysPerMonth`) | 20 日/月 | **デフォルト値（可変）** | 就労条件総合調査 令和7年（令和6年実績）における中小企業（30〜99 人）の年間休日 111.2 日 + 年休取得 12.1 日 = 実稼働 約 240 日/年 = 20 日/月。詳細出典は §3 参照。 |

### 2.1 UI への影響
- 通常フォームは 5 項目のまま（#2 の設計前提）。
- 「時給 / 1日作業時間 / 月稼働日数」の 3 項目は **「詳細設定」折り畳み UI** として配置し、デフォルトでは閉じた状態。
- 折り畳みを開かない限り、デフォルト値が使用される。
- なお、詳細設定の具体的なラベル・プレースホルダ・バリデーション範囲等の UI 詳細は **Issue #2 の設計スコープ**。本仕様書では「何を可変にするか」「デフォルト値として何を使うか」のみを確定する。

---

## 3. デフォルト値の出典（説明責任用）

商談で「なぜこの値なのか」を問われた時に即答できる、公的統計・業界調査ベースの根拠一覧。

> Issue #90（2026-05-04 改定）で、旧版「説明ロジック（1 行）」から統計出典付きの完全版に置換完了。Phase 1 リサーチノート（`working/issue-90/research/public-stats.md` / `industry-reports.md`）が一次情報源。

### 3.1 時給 2,500 円/時

| 項目 | 内容 |
|---|---|
| 統計名 | 賃金構造基本統計調査 令和7年速報（令和6年確報含む） |
| 発行者 | 厚生労働省 |
| URL | https://www.mhlw.go.jp/toukei/itiran/roudou/chingin/kouzou/z2025/sokuhou.html |
| 取得日 | 2026-05-04 |
| 採用ロジック | 中小企業（10〜99 人）一般労働者の所定内給与額（月額 約 30〜33 万円）に、賞与按分（×1.2）、社会保険料事業主負担（×1.15）、所定内実労働時間（月 170 時間）を加味し、実質時給レンジ **2,500〜2,800 円** を算出。商談時に「過大な ROI を出している」と疑われない保守的下限値として **2,500 円** を採用。 |
| 旧版との差分 | 旧版「中小企業の平均的なホワイトカラー人件費を参照した標準値」（出典なし）→ 公的統計の所定内給与額に法定費用を加味した実質時給として再定義。**数値は同値（2,500 円）** だが根拠の説明責任を強化。 |

### 3.2 1 日あたり手作業時間 2 時間/日

| 項目 | 内容 |
|---|---|
| 調査名 | 会社員の業務実態調査 |
| 発行者 | BPO テクノロジー株式会社 |
| URL | https://prtimes.jp/main/html/rd/p/000000011.000086224.html |
| 取得日 | 2026-05-04 |
| 採用ロジック | ビジネスパーソンの約 3 人に 1 人が 1 日 30 分〜5 時間をノンコア業務（サブ業務）に従事。1 日所定労働時間（企業平均 7 時間 47 分）における **10〜60% がノンコア業務**で、中央値レンジは **約 1〜3 時間/日**。AI 自動化で代替可能な定型業務に絞った中央値として **2 時間/日** を採用。 |
| 補完出典 | コクヨ「ビジネスパーソンが調べものに費やす時間 = 1.6 時間/日」 https://www.kokuyo-furniture.co.jp/solution/mana-biz/2019/11/16.php |
| 旧版との差分 | 旧版「定型業務・手作業の 1 人あたり平均所要時間として業界で一般的に使われる目安値」（出典なし）→ 業務実態調査の中央値レンジから採用根拠を再定義。**数値は同値（2 時間/日）**。 |

### 3.3 月あたり稼働日数 20 日/月

| 項目 | 内容 |
|---|---|
| 統計名 | 令和7（2025）年就労条件総合調査の概況 |
| 発行者 | 厚生労働省 |
| URL | https://www.mhlw.go.jp/toukei/itiran/roudou/jikan/syurou/25/dl/gaikyou.pdf |
| 取得日 | 2026-05-04 |
| 採用ロジック | 中小企業（30〜99 人）の **1 企業平均年間休日総数 111.2 日**、年休取得率 66.9%、平均取得日数 12.1 日。実稼働日 = 365 − 111.2 − 12.1 = **241.7 日/年** ÷ 12 = **約 20.1 日/月**。 |
| 旧版との差分 | 旧版「標準的な営業日数（週5日 × 4週）」（出典なし）→ 公的統計の年間休日総数から実稼働日を逆算する形に再定義。**数値は同値（20 日/月）**。 |

### 3.4 年あたり改修回数 3 回/年

| 項目 | 内容 |
|---|---|
| 出典 | システム保守業界の通念（業界統計値は公開情報からは未取得） |
| 補完情報 | システム幹事「システム開発の保守費用相場・内訳具体例 2026 年最新版」 https://system-kanji.com/posts/system-development-maintenance-cost-quote |
| 採用ロジック | 中小企業 Web/業務システムの典型的な改修頻度として、**四半期ごとの定期改修＋軽微対応 1 回除外 = 年 3 回**。中小企業向け SaaS／業務システムの保守契約相場（年間保守費 = 開発費の 15〜20%）と整合。 |
| 旧版との差分 | 旧版と説明ロジック同一。**数値は同値（3 回/年）**。業界統計データが公開情報から取得困難なため、業界通念を維持。業種別係数の導入は §8「未解決事項 / 将来拡張」に残置。 |

### 3.5 試算期間 36 ヶ月（3 年）

| 項目 | 内容 |
|---|---|
| 出典 | 商品コンセプト「3 年間の止血」（マスター設計書 §3.1） |
| 採用ロジック | 中期経営計画の標準期間（多くの企業で採用される 3 ヶ年計画）と一致。可変化は商品設計上不可。 |

### 3.6 内製化 5 段階（0 / 0.25 / 0.5 / 0.75 / 1.0）

| 項目 | 内容 |
|---|---|
| 統計名 | 2025 年版「中小企業白書」 |
| 発行者 | 中小企業庁 |
| URL | https://www.chusho.meti.go.jp/pamflet/hakusyo/2025/PDF/2025gaiyou.pdf |
| 取得日 | 2026-05-04 |
| 採用ロジック | 中小企業の IT 内製化率の精密な分布データは白書からは取得困難。ただし「ベンダー丸投げ」が支配的（企業間取引の約 70% が電話・FAX・電子メール）で、デジタル化進展企業も約 30% 存在する実態から、**5 段階（完全依存／一部内製／半分／大半／完全）は商談現場での粒度として過不足ない**と判断。粒度を 7 段階以上に細分化すると商談テンポ（§1.2）を損なうため見送り。 |
| 旧版との差分 | 構造変更なし。粒度の妥当性検証を Issue #90 で実施済みと明記。 |

### 3.7 出典更新ポリシー

- 公的統計（賃金構造基本統計調査・就労条件総合調査）は厚生労働省が**毎年 6〜10 月頃**に更新するため、**少なくとも 1 年に 1 回**の見直しを推奨。
- 採用値の変更が必要と判断された場合は、本仕様書改定 → `src/lib/constants.ts` 更新 → 関連テスト期待値更新 → リリースノートで「破壊的変更（過去配布 PDF との数値乖離）」として告知する手順（§10.5「移行ノート」参照）。
- 営業／顧客ヒアリング結果（`working/issue-90/research/customer-interviews.md` 参照）が「現行値が実態と乖離」を示した場合は、別 Issue を起票して再リファクタリング。

---

## 4. 「内製化状況」の選択肢と係数定義

### 4.1 選択肢
5 段階のセグメントコントロール（またはラジオボタン）で提示する。

| ラベル | 内部値 (`insourcingLevel`) | 係数 (`insourcingGap`) |
|---|---|---|
| 完全ベンダー依存 | `0` | `1.0` |
| 一部内製 | `0.25` | `0.75` |
| 半分内製 | `0.5` | `0.5` |
| 大半内製 | `0.75` | `0.25` |
| 完全内製 | `1.0` | `0.0` |

### 4.2 計算式への反映方法
- **係数として「3年間の止血」に乗算する**（方式 ii）。
- 計算式: `insourcingGap = 1 - insourcingLevel`
- 説明ロジック: 「既に内製化されている割合分は既に自社内でコストが吸収済みなので、ベンダー流出コストからは除外」。
- **副次効果（数値の盛り問題への対応）**: 係数なしの素計算だと「月額100万円 → 3年で3,600万円削減」のように過大な数値が出うるが、内製化係数の導入により、理想形に近い顧客（既に内製化が進んだ企業）ほど自然にキャリブレーションされ、誠実な結果が提示される。

### 4.3 「創出」には乗算しない理由
- AI 自動化による手作業削減（創出）は、ベンダー委託の有無と独立した効果。
- 完全内製の企業でも手作業は発生しうるため、創出額は内製化状況に影響されない。

### 4.4 カテゴリ別表示文の切替（併用）
計算式への係数化に加え、カテゴリごとに訴求文を出し分ける（Issue #4 で詳細文言を確定）。

| カテゴリ | 表示方針 |
|---|---|
| 完全ベンダー依存 / 一部内製 | 「削減余地大」の訴求 |
| 半分内製 / 大半内製 | 「更なる効率化の提案」訴求 |
| 完全内製 | 「既に理想形、維持フェーズ」として誠実に表示 |

---

## 5. 最終計算式（擬似コード）

> 本擬似コードが Issue #6 以降の実装で参照される正本です。

```ts
// ───────────────── 定数（固定） ─────────────────
const MONTHS_IN_PERIOD = 36;        // 試算期間（3年）
const YEARS_IN_PERIOD = 3;
const REPAIRS_PER_YEAR = 3;         // 年あたり改修回数
const MONTHS_PER_YEAR = 12;

// ───────────────── デフォルト値（ハイブリッドで上書き可） ─────────────────
const DEFAULT_HOURLY_WAGE = 2500;    // 円/時
const DEFAULT_HOURS_PER_DAY = 2;     // 時間/日
const DEFAULT_DAYS_PER_MONTH = 20;   // 日/月

// ───────────────── ユーザー入力 ─────────────────
type InsourcingLevel = 0 | 0.25 | 0.5 | 0.75 | 1.0;

interface Inputs {
  // 基本5項目
  monthlyVendorCost: number;           // 月額ベンダー費用（円）
  repairCost: number;                  // 単発改修費用（円）
  manualWorkerCount: number;           // 手作業人数
  updateWaitMonths: number;            // 更新待ち期間（月）
  insourcingLevel: InsourcingLevel;    // 内製化状況

  // 詳細設定（オプション。未指定時はデフォルト値）
  hourlyWage?: number;
  hoursPerDay?: number;
  daysPerMonth?: number;
}

interface CalculationResult {
  threeYearSavings: number;            // 3年間の止血（削減額）
  annualProfitCreation: number;        // 年間の利益創出額
  threeYearProfitCreation: number;     // 3年間の利益創出額
  totalThreeYearImpact: number;        // 3年間のトータルインパクト（合算）
  speedWarning: boolean;               // スピード警告発動フラグ
  insourcingGap: number;               // 内製化ギャップ係数（0〜1）
}

function calculate(input: Inputs): CalculationResult {
  const hourlyWage = input.hourlyWage ?? DEFAULT_HOURLY_WAGE;
  const hoursPerDay = input.hoursPerDay ?? DEFAULT_HOURS_PER_DAY;
  const daysPerMonth = input.daysPerMonth ?? DEFAULT_DAYS_PER_MONTH;

  // 内製化ギャップ係数（既に内製化された分は削減余地から除外）
  const insourcingGap = 1 - input.insourcingLevel;

  // 3年間の止血（削減額）: 内製化ギャップを乗算
  const rawSavings =
    input.monthlyVendorCost * MONTHS_IN_PERIOD +
    input.repairCost * REPAIRS_PER_YEAR * YEARS_IN_PERIOD;
  const threeYearSavings = rawSavings * insourcingGap;

  // 年間の利益創出額: 内製化係数は乗算しない
  const annualProfitCreation =
    input.manualWorkerCount *
    hoursPerDay *
    daysPerMonth *
    MONTHS_PER_YEAR *
    hourlyWage;

  const threeYearProfitCreation = annualProfitCreation * YEARS_IN_PERIOD;

  return {
    threeYearSavings,
    annualProfitCreation,
    threeYearProfitCreation,
    totalThreeYearImpact: threeYearSavings + threeYearProfitCreation,
    speedWarning: input.updateWaitMonths >= 3,
    insourcingGap,
  };
}
```

---

## 6. 表示・丸めルール

| 項目 | ルール |
|---|---|
| 内部計算 | 円単位・浮動小数で保持（精度を落とさない） |
| UI 表示単位 | **万円単位** で丸めて表示（経営者向けに可読性優先） |
| 丸め方 | **四捨五入**。例: 135 万円を得る場合 `Math.round(value / 10000)`（万円単位の整数を返す。UI では後段で「万円」単位を付与する） |
| ゼロ値 | `0` 円でもそのまま表示（ハイフン等に置換しない） |
| 負値 | 計算上は発生しない前提。発生時は `0` として表示し、バリデーション強化を検討 |

### 6.1 表示項目
- ダッシュボード: 「3年間の止血」「年間の利益創出」「3年間のトータルインパクト」を並列表示
- PDF サマリー: 3年間のトータルインパクトを表紙訴求
- 注記: UI には「試算上の最大値」である旨を明記する

---

## 7. スピード警告の発動条件

- 条件: `updateWaitMonths >= 3`（3ヶ月以上で発動）
- 表示: マスター設計書通り「CRITICAL OPPORTUNITY LOSS」を含む警告 UI（文言の最終版は Issue #4 で確定）

---

## 8. 未解決事項 / 将来拡張

本 Issue のスコープ外として記録し、必要に応じて別 Issue 化する。

### plan 由来（plan §4 リスクから持ち越し）
- **業種別プリセット**: 製造業・IT業・小売業等で時給/稼働日数のプリセットを持つ拡張（plan R3 由来）。Issue #90 のヒアリング結果で「業種ごとの ROI 乖離」が示された場合に着手。
- ~~**統計出典の置き換え**: 現在の説明ロジックを、公的統計データ（賃金構造基本統計調査等）で裏付ける作業（plan R4 由来）。~~ → **Issue #90（2026-05-04）で完了**。§3 参照。

### 本仕様書で追加した論点（plan にはなかった）
- **多通貨対応**: 現時点では円のみ。
- **年度を跨ぐ価格改定への対応**: ベンダー費用が年度で変動する場合の取り扱い。
- **業界別係数 / 時間価値割引率 / リスク調整係数**: Issue #90 で導入を検討したが、業界別の確定的なデータが公開情報から取得できず、商談現場での説明難度を上げるため見送り。営業ヒアリング（`working/issue-90/research/customer-interviews.md`）で「過大」「過小」のシグナルが集まった場合に再検討。

---

## 9. 決定項目チェックリスト

本仕様書で確定済みの項目：

### 固定値
- [x] 時給 2,500 円: **デフォルト値（可変）**
- [x] 1 日 2 時間: **デフォルト値（可変）**
- [x] 月 20 日: **デフォルト値（可変）**
- [x] 年 3 回改修: **固定**
- [x] 36 ヶ月（3年）: **固定**
- [x] 各デフォルト値の根拠（1行説明）記録済み → §3

### 内製化状況
- [x] 選択肢の数と粒度: **5 段階**（0 / 0.25 / 0.5 / 0.75 / 1.0）
- [x] 各選択肢のラベル文言確定 → §4.1
- [x] 計算式への反映方法: **係数化**（§4.2）＋ **カテゴリ別表示文切替**（§4.4）併用
- [x] 係数の乗算対象: **「止血」のみ**、「創出」には乗算しない
- [x] 完全内製（100%）顧客への表示方針: 「維持フェーズ」として誠実表示

### 計算式
- [x] 「3年間の止血」最終式記載済み → §5
- [x] 「年間の利益創出額」最終式記載済み → §5
- [x] 「3年間の利益創出額」を出す（= 年間 × 3）
- [x] 「3年間のトータルインパクト（合算）」を出す
- [x] スピード警告の発動条件: `updateWaitMonths >= 3`（以上）
- [x] 丸め単位: **万円**、丸め方: **四捨五入** → §6

### ドキュメント
- [x] 仕様書の保存先: `docs/spec/calculation-logic.md`
- [x] マスター設計書との参照関係記載
- [ ] **[TODO / マージ後]** Issue #2〜#5 の説明欄に「仕様の前提は `docs/spec/calculation-logic.md` を参照」と追記（plan §6 手順6 に対応する運用作業。本 PR マージ後のフォローアップで実施）

---

## 9.5 移行ノート（Issue #90 改定: 2026-05-04）

### 9.5.1 v1（旧版） vs v2（本改定）の数値比較

| 標準入力（仕様書 §5 @example） | v1 旧版の計算結果 | v2 本改定の計算結果 | 差分 |
|---|---|---|---|
| 月額 100 万円 / 改修 50 万円 / 5 人 / 4.5 ヶ月 / 一部内製 (0.25) | `threeYearSavings: 30,375,000` 円 / `threeYearProfitCreation: 18,000,000` 円 / `totalThreeYearImpact: 48,375,000` 円 | `threeYearSavings: 30,375,000` 円 / `threeYearProfitCreation: 18,000,000` 円 / `totalThreeYearImpact: 48,375,000` 円 | **同一（数値変更なし）** |

### 9.5.2 過去配布 PDF との数値乖離

- **v1 → v2 で数値変更は発生しなかった**。Issue #90 のリサーチ結果（公的統計の中央値レンジに現行値が収まる）により、定数値は据え置きとなった。
- したがって、**過去に配布した PDF と本リリース後に再生成される PDF の計算結果は完全に一致する**。
- 商談時に「旧版と新版で同じ入力に対して同じ ROI が出るか」を確認されたら、**「同じ結果が出ます。本改定は数値変更ではなく、根拠の出典明記による説明責任強化です」**と回答する。

### 9.5.3 商談時の説明強化ポイント

本改定で営業／商品担当が新たに使えるようになる説明:

1. **時給 2,500 円**: 厚労省「賃金構造基本統計調査 令和7年速報」の所定内給与額に賞与・社会保険料事業主負担を加味した、中小企業ホワイトカラー実質時給レンジ（2,500〜2,800 円）の保守的下限値。
2. **月稼働 20 日**: 厚労省「就労条件総合調査 令和7年」の中小企業（30〜99 人）年間休日 111.2 日 + 年休取得 12.1 日 = 実稼働 約 240 日/年 = 20 日/月。
3. **手作業 2 時間/日**: BPO テクノロジー「会社員の業務実態調査」のノンコア業務時間中央値レンジ（1〜3 時間/日）の中央値。

### 9.5.4 今後のメンテナンス

- 公的統計（賃金構造基本統計調査・就労条件総合調査）が毎年 6〜10 月頃に更新されるため、年 1 回のレビューサイクルで本仕様書 §3 の取得日・URL・採用ロジックを再確認する。
- 統計値が大きく変動した（採用値レンジから外れた）場合は、本仕様書改定 → `src/lib/constants.ts` 更新 → 関連テスト期待値更新 → リリースノートで「破壊的変更（過去配布 PDF との数値乖離）」として告知する手順を踏む。

---

## 10. 関連 Issue / 後続作業

| Issue | 本仕様書の参照箇所 |
|---|---|
| #2 入力フォーム 5 項目の UI 仕様 | §2（固定値扱い）、§4.1（内製化選択肢）、§5（Inputs 型） |
| #3 診断結果グラフの種別 | §5（CalculationResult 型）、§6（表示単位） |
| #4 警告コピー文言 | §4.4（カテゴリ別表示文）、§7（スピード警告条件） |
| #5 PDF レポートレイアウト | §5（出力項目）、§6（表示ルール）。**確定仕様は [`pdf-report.md`](./pdf-report.md) を参照（Issue #5 で確定）** |
| #6 Next.js 雛形作成後 | §5 の擬似コードを `src/lib/constants.ts` / `src/lib/calculation.ts` として実装。`InsourcingLevel` は `as const` 配列からリテラル union 型を導出する／Zod 等のバリデーションで入力を締める等、実行時・型の両面から堅牢化することを推奨 |
| #90 計算ロジック前提値・計算式の現実即応リファクタリング | §2（出典付き理由列）／§3（公的統計出典セクション）／§9.5（移行ノート）。**本 PR で完了**。 |
