# 事前準備イシュー 実施順プラン

「またたび計算機」実装に着手する前に片付けるべき準備タスクを、依存関係と着手効率を踏まえて並べた一覧。
原則として **上から順に実施**。並列で進められるものは「並行可」と明記。

> **進捗ステータス凡例**: ✅ 完了 / 🟡 進行中 / ⏳ 未着手

**現在地（2026-04-25 時点）**: フェーズ 0（仕様確定）と フェーズ 1（雛形）を完了。次はフェーズ 2 / 3 / 4 の並行進行および本実装フェーズへの移行。

---

## フェーズ 0: 仕様確定（実装前に必ず決める）

コードを書き始める前に固めるべき意思決定。ここが揺れると後戻りコストが大きい。

| # | 状態 | タイトル | 概要 | 依存 |
|---|------|---------|------|------|
| 1 | ✅ | 計算ロジックの前提値を確定する | 時給2,500円／1日2時間／月20日／年3回改修など固定値の是非、「内製化状況」の選択肢と係数を決定 → `docs/spec/calculation-logic.md` | なし |
| 2 | ✅ | 入力フォーム5項目のUI仕様を確定する | 数値入力／レンジ／セレクトの別、単位、バリデーション範囲を定義 → `docs/spec/input-form.md` | #1 |
| 3 | ✅ | 診断結果グラフの種別とライブラリを選定する | 積み上げ横棒1本 / **Recharts v2 採用**を確定 → `docs/spec/result-dashboard.md` | #1 |
| 4 | ✅ | 警告コピー「CRITICAL OPPORTUNITY LOSS」等の文言を確定する | 文言・表示トリガ条件の最終決定 → `docs/spec/warning-copy.md` | #1 |
| 5 | ✅ | PDFレポートのレイアウト仕様を確定する | A4 1枚の項目配置、html2canvas 主体 / `next/font/local` 方針 → `docs/spec/pdf-report.md` | #1, #3 |

---

## フェーズ 1: リポジトリ / プロジェクト雛形

仕様が固まったらコードベースを用意する。

| # | 状態 | タイトル | 概要 | 依存 |
|---|------|---------|------|------|
| 6 | ✅ | Next.js 14+ プロジェクト雛形を作成する | create-next-app で App Router / TypeScript / Tailwind / ESLint を有効化。.gitignore / README / tsconfig 整備（後に Issue #53 / Issue #64 で `next@16` へ更新） | フェーズ0完了 |
| 7 | ✅ | Tailwind CSS のデザイントークンを設定する | ベースカラー（オフホワイト #F8F6F2 等）を `theme.extend.colors` に登録。Inter / Noto Sans JP を `next/font/google` で読込 | #6 |
| 8 | ✅ | 依存ライブラリを追加する (lucide-react / jspdf / html2canvas / recharts) | バージョン固定、型定義の要否確認。**実版**: lucide-react `^0.460.0` / jspdf `^2.5.2` / html2canvas `^1.4.1` / recharts `^2.15.4` | #6, #3 |

---

## フェーズ 2: ブランド資産 / 共通基盤

雛形と並行して進められる。デザイン素材が揃うとUI実装がスムーズ。

| # | 状態 | タイトル | 概要 | 依存 | 備考 |
|---|------|---------|------|------|------|
| 9 | ⏳ | ロゴ・猫モチーフ素材を用意する | ファビコン、OGP画像、背景装飾SVGの入手または作成 | なし | フェーズ1と並行可 |
| 10 | ⏳ | デザインガイドラインをコードに落とし込む | カラートークン、余白、角丸、シャドウの共通値を `globals.css` / Tailwind プリセットへ | #7 | |
| 22 | ⏳ | 既存 spec ドキュメントの旧パレット参照を新パレットへ置換する | `docs/spec/*.md` 内の旧色コード（#3B82F6 等）を #7 で確定した新パレットに合わせて修正 | #7 | spec の整合性メンテ |

---

## フェーズ 3: インフラ / デプロイ

最初のコードが動き出したら早めに配信経路を通す（空のページでもOK）。

| # | 状態 | タイトル | 概要 | 依存 |
|---|------|---------|------|------|
| 11 | ⏳ | Cloudflare Pages プロジェクト作成と GitHub 連携 | ビルドコマンド／出力先の設定。`@cloudflare/next-on-pages` 採用か静的出力かを決定。プレビュー/本番ブランチ運用ルールも決める | #6 |
| 12 | ⏳ | 独自ドメインを設定する | Cloudflare DNS、SSL/TLS モード (Full等) の方針確定と適用 | #11 |

---

## フェーズ 4: 法務 / 運用

公開前に必ず整える。実装と並行して進めておくと後が楽。

| # | 状態 | タイトル | 概要 | 依存 |
|---|------|---------|------|------|
| 13 | ⏳ | プライバシーポリシー / 利用規約の要否を判断し文面を用意する | 入力値の送信・保存有無を明示（完全クライアント計算ならその旨） | なし |
| 14 | ⏳ | アクセス解析の方針を決めて導入する | GA4 または Cloudflare Web Analytics の採用判断と計測タグ設置 | #11 |

---

## フェーズ 5: 本実装フェーズ前のフォローアップ

本実装着手前に解決しておくべき調整事項。

| # | 状態 | タイトル | 概要 | 依存 |
|---|------|---------|------|------|
| 24 | ⏳ | jspdf v2 系の脆弱性追跡と Issue #5 着手前の対応方針確定 | `npm audit` で報告された jspdf 由来 critical/high 脆弱性の到達経路調査 / v3 移行 PoC / CI audit-level 設定の判断 | #8 |

---

## 本実装フェーズ（未起票）

事前準備が完了次第、以下の本実装 Issue を順次起票する想定（現時点では Issue 化されていない）:

- 共通ユーティリティ実装（`src/lib/calculation.ts` / `src/lib/format.ts` / `src/lib/constants.ts` 等）
- `InputForm` 実装（仕様: `docs/spec/input-form.md`）
- `ResultDashboard` / `DashboardView` 実装（仕様: `docs/spec/result-dashboard.md`、依存: #24 完了）
- `WarningBanner` 実装（仕様: `docs/spec/warning-copy.md`）
- `PdfDashboard` / `src/lib/pdf.ts` 実装（仕様: `docs/spec/pdf-report.md`、依存: #24 完了）
- `useCountUp` フック実装

---

## 推奨スケジュール（目安・進捗反映済み）

- **Week 1**: フェーズ0（#1〜#5）を全て確定 — ✅ 完了
- **Week 2**: フェーズ1（#6〜#8）— ✅ 完了
- **Week 3（現在）**: フェーズ2（#9, #10, #22）+ フェーズ3（#11）+ フェーズ4（#13）を並行
- **Week 4 以降**: #12, #14, #24 を片付け、本実装フェーズ着手

---

## ガントチャート風 依存関係図

```
[完了]
#1 ─┬─ #2 ✅
    ├─ #3 ── #5 ✅
    ├─ #4 ✅
    └─ (全て) ── #6 ─┬─ #7 ✅
                     └─ #8 ✅

[未着手 / 並行可]
#7 ── #10 ⏳
   └─ #22 ⏳
#6 ── #11 ─┬─ #12 ⏳
           └─ #14 ⏳
#8 ── #24 ⏳ ── (本実装フェーズの PDF / Dashboard 着手の前提)

[独立]
#9  ⏳
#13 ⏳
```
